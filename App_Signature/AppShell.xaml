<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="App_Signature.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:App_Signature.Views"
    Shell.FlyoutBehavior="Disabled">

    <ShellContent Title="Home" ContentTemplate="{DataTemplate local:MainView}" Route="MainView" />
    <!--just define the global views, the others are defined in the appshell.xaml.cs-->
    <ShellContent Title="Trip" ContentTemplate="{DataTemplate local:TripView}" Route="TripView" />
</Shell>
